pipeline {
    agent any
    stages {
        stage('Call API') {
            steps {
                sh 'chmod oug+x ./visium.sh'
                sh './visium-ios.sh'
            }
        }
    }
}
